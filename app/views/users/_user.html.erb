<li>
	<%= gravatar_for user, size: 52 %>
	<%= link_to user.full_name, user %>

	<% if current_user.admin? && !current_user?(user) %>
		<%= link_to "Elimina", user, method: :delete,
		data:  { confirm: "Sei sicuro?" } %>
	<% end %>
</li>